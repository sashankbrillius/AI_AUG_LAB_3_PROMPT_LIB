global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: "prompt-library"
    metrics_path: /metrics
    static_configs:
      - targets: ["prompt-library:7000"]
        labels:
          service: "prompt-library"
